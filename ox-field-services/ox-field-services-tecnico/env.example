# Google Maps JavaScript API key (required for the map on the task execution screen).
# Create a key at https://console.cloud.google.com/apis/credentials and enable "Maps JavaScript API".
# Restrict the key by HTTP referrer to your app domain for production.
VITE_GOOGLE_MAPS_API_KEY=

# Optional: Map ID for Advanced Markers (avoids deprecated Marker). Use DEMO_MAP_ID for testing,
# or create a map ID in Google Cloud Console (Map Management) for production.
# VITE_GOOGLE_MAPS_MAP_ID=DEMO_MAP_ID
